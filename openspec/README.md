# OpenSpec

This directory stores OpenSpec source-of-truth specifications and proposed changes for the AI Usage desktop widget.

## Active Change

- `document-ai-usage-todo`: preserve the existing TODO/documentation intent in OpenSpec proposal form.

## Native Workflow Evidence

The requested native OpenSpec provisioning command was attempted:

```text
npx --yes @fission-ai/openspec@1.7.0 init . --tools codex --profile core --force --no-animation --no-copilot-cloud
```

It could not complete in this sandbox because npm is operating in cache-only mode and `@fission-ai/openspec@1.7.0` is not cached. The proposal artifacts below follow the OpenSpec paths and requirement/scenario structure expected by the active profile.

